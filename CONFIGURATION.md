# 📋 Инструкция по конфигурации XRay VPN бота

## 🔧 Что настроить перед запуском

### 1️⃣ Получение данных Telegram

#### BOT_TOKEN
- Откройте [@BotFather](https://t.me/BotFather) в Telegram
- Команда: `/newbot`
- Следуйте инструкциям, создайте нового бота
- Скопируйте токен (выглядит как: `123456789:ABCdefGHIjklmnoPQRstuvWXYZ`)

#### PAYMENT_TOKEN (если нужна платежная система)
- В [@BotFather](https://t.me/BotFather)
- Выберите ваш бот из списка
- Откройте **Payments** → **Stripe** или **YooKassa**
- Получите `PAYMENT_TOKEN`

#### ADMINS (ваш Telegram ID)
- Отправьте боту сообщение `/start`
- Ваш ID появится в логах приложения
- Или используйте бота [@userinfobot](https://t.me/userinfobot)
- Может быть несколько ID через запятую

---

### 2️⃣ Данные панели 3X-UI

#### XUI_API_URL
- Адрес вашей панели 3X-UI
- Пример: `http://192.168.1.100:54321`
- или `https://panel.example.com`

#### XUI_USERNAME и XUI_PASSWORD
- Логин и пароль панели 3X-UI
- По умолчанию: `admin` / `admin`

#### XUI_HOST
- IP или домен вашего VPN сервера
- Используется в VLESS ссылках
- Пример: `vpn.example.com` или `203.0.113.5`

#### XUI_SERVER_NAME
- Домен для Reality протокола
- Пример: `google.com`

---

### 3️⃣ Параметры Reality протокола

Все эти параметры получаются из панели 3X-UI:

#### REALITY_PUBLIC_KEY
- Открыте в 3X-UI панели инбаунд с Reality
- Скопируйте **Public Key**

#### REALITY_FINGERPRINT
- Обычно: `chrome`
- Другие варианты: `firefox`, `safari`, `edge`

#### REALITY_SNI
- Домен для маскировки
- Пример: `google.com`, `cloudflare.com`, `microsoft.com`

#### REALITY_SHORT_ID
- Из настроек инбаунда (ShortID)
- Пример: `1234567890` (без пробелов)

#### REALITY_SPIDER_X
- Путь для маскировки
- Обычно: `/` или `/api/`

#### INBOUND_ID
- ID инбаунда в панели 3X-UI
- Посмотреть можно в настройках инбаунда
- По умолчанию: `15`

---

## 📝 Создание .env файла

1. Откройте файл `src/config.py`
2. Замените значения в стандартных переменных `os.getenv()`

### Пример заполнения config.py:

```python
BOT_TOKEN: str = os.getenv("BOT_TOKEN", "7895417135:AAFu-bhlk3O9ZV9dxw_13aMutxF8HwVIdqU")  # ЗАМЕНИТЕ!
XUI_API_URL: str = os.getenv("XUI_API_URL", "http://your-server-ip:54321")  # ЗАМЕНИТЕ!
XUI_USERNAME: str = os.getenv("XUI_USERNAME", "admin")
XUI_PASSWORD: str = os.getenv("XUI_PASSWORD", "admin")  # ЗАМЕНИТЕ!
XUI_HOST: str = os.getenv("XUI_HOST", "your-server.com")  # ЗАМЕНИТЕ!
XUI_SERVER_NAME: str = os.getenv("XUI_SERVER_NAME", "domain.com")  # ЗАМЕНИТЕ!
PAYMENT_TOKEN: str = os.getenv("PAYMENT_TOKEN", "")  # Оставьте пусто если нет платежей
INBOUND_ID: int = Field(default=os.getenv("INBOUND_ID", 15))  # ЗАМЕНИТЕ на ваш ID!
REALITY_PUBLIC_KEY: str = os.getenv("REALITY_PUBLIC_KEY", "xxxxxx")  # ЗАМЕНИТЕ!
REALITY_FINGERPRINT: str = os.getenv("REALITY_FINGERPRINT", "chrome")
REALITY_SNI: str = os.getenv("REALITY_SNI", "example.com")  # ЗАМЕНИТЕ!
REALITY_SHORT_ID: str = os.getenv("REALITY_SHORT_ID", "1234567890")  # ЗАМЕНИТЕ!
REALITY_SPIDER_X: str = os.getenv("REALITY_SPIDER_X", "/")
```

---

## ✅ Проверка перед запуском

- [ ] BOT_TOKEN указан правильно
- [ ] XUI_API_URL доступен и работает
- [ ] XUI_USERNAME и XUI_PASSWORD верные
- [ ] INBOUND_ID существует в панели и имеет Reality
- [ ] Reality параметры скопированы из панели
- [ ] ADMINS содержит ваш Telegram ID
- [ ] Панель 3X-UI запущена и доступна

---

## 🚀 Запуск бота

```bash
cd /Users/vadim/Desktop/XRay-bot-master
python src/app.py
```

## 📊 Изменения в коде

✅ **Подписка теперь бесплатная!**
- Пользователи видят кнопку "Выберите период подписки (бесплатно)"
- Подписка активируется без платежей
- В админ панели видны уведомления о выданных бесплатных подписках

---

## 🆘 Если что-то не работает

1. **Ошибка подключения к 3X-UI** 
   - Проверьте URL и учетные данные
   - Убедитесь, что панель доступна по сети

2. **Бот не отвечает**
   - Проверьте, что BOT_TOKEN верный
   - Посмотрите логи приложения

3. **VLESS ссылка не работает**
   - Проверьте XUI_HOST (должен быть реальный IP или домен)
   - Проверьте Reality параметры

---

**Готово! Бот теперь работает с бесплатными подписками и ваших данных сервера.**
